# ==========================================
# HABIT TRACKER - ENVIRONMENT VARIABLES
# ==========================================

# Node Environment (development or production)
NODE_ENV=development

# ==========================================
# SERVER CONFIGURATION
# ==========================================

# Server Port
PORT=3000

# Session Secret (REQUIRED - use a strong random string in production)
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your-secret-key-here-change-in-production

# ==========================================
# DATABASE CONFIGURATION
# ==========================================

# MongoDB Connection String (REQUIRED)
# Local: mongodb://localhost:27017/habit-tracker
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/habit-tracker?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/habit-tracker

# ==========================================
# FIREBASE CONFIGURATION (Optional - for Google Auth)
# ==========================================

# Firebase Project Configuration
# Get these from Firebase Console > Project Settings > General
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789012
FIREBASE_APP_ID=1:123456789012:web:abcdef123456
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Firebase Admin SDK Configuration
# Get this from Firebase Console > Project Settings > Service Accounts
# Path to your service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json

# ==========================================
# EMAIL CONFIGURATION (Optional)
# ==========================================

# Email service configuration for notifications
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# ==========================================
# PRODUCTION DEPLOYMENT NOTES
# ==========================================
# 
# 1. Change SESSION_SECRET to a strong random value
# 2. Use production MongoDB connection string (MongoDB Atlas)
# 3. Add Firebase configuration for Google Auth
# 4. Enable HTTPS in production
# 5. Set NODE_ENV=production
# 6. Never commit .env file to version control
#
